<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Invoice</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      margin: 0;
      padding: 0;
      background-color: #f4f4f4;
    }

    h1 {
      text-align: center;
      color: #333;
    }

    p, ul {
      max-width: 600px;
      margin: 10px auto;
      padding: 10px;
      background-color: #fff;
      border-radius: 5px;
      box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
    }

    ul {
      list-style-type: none;
      padding: 0;
    }

    li {
      padding: 10px;
      border-bottom: 1px solid #ccc;
    }

    li:last-child {
      border-bottom: none;
    }

    a {
      color: #007bff;
      text-decoration: none;
    }

    a:hover {
      text-decoration: underline;
    }

    h2 {
      text-align: center;
      color: #333;
    }

    h2 + ul, h2 + p {
      margin-top: 0;
    }
  </style>
</head>
<body>
  <h1>Invoice</h1>
  <p>Thank you for your order, {{invoice.user_fname}} {{invoice.user_lname}}</p>
  <p>Total Cost: ${{invoice.total_cost}}</p>
  <p>Taxes: ${{invoice.taxes}}</p>
  <p>Delivery: {{#if invoice.delivery}}Yes{{else}}No{{/if}}</p>
  <p>Credit Card: {{invoice.masked_credit_card}}</p>

  <h2>Products</h2>
  <ul>
    {{#each invoice.products}}
      <li>{{this.prod_name}} - ${{this.prod_price}}</li>
    {{/each}}
  </ul>

  <h2>Review Your Restaurants</h2>
  <ul>
    {{#each invoice.restaurants}}
      <li>
        {{this.name}} - <a href="/review/{{this.src_id}}">Leave a review</a>
      </li>
    {{/each}}
  </ul>
</body>
</html>
